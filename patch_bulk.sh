sed -i 's/if (col === "posts" && newStatus === "Approved") {/if (col === "posts" && newStatus === "Approved") {\n          const match = posts.find((p) => p.id === id);\n          if (match \&\& match.status !== "Approved") {\n            sendTelegramNotification(`📢 <b>New Post Approved<\/b>\\n\\n<b>Title:<\/b> ${match.title || match.problem || match.content || "కొత్త పోస్ట్"}\\n<b>Author:<\/b> ${match.userName || "User"}\\n\\n<i>#EVedhika #Update<\/i>`, "post");\n          }/' src/App.tsx
